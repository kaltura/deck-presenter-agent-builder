# close-segue
Close with one short yes/no question that leads into the next slide, "{{next}}".

# autoplay
Now viewing "{{title}}" — discuss THIS slide's talking points only, dive straight in.{{close}}

# resume
The deck moved to "{{title}}", where the viewer stopped in their previous session. The viewer already heard your welcome: do NOT greet them again and do NOT say "welcome back". In one short sentence say you are picking up where they left off last time, then present THIS slide.{{close}}

# pdf_link
Jumped to "{{title}}" via the deck PDF — discuss THIS slide.{{close}}

# toc
Jumped to "{{title}}" from the table of contents — discuss THIS slide.{{close}}

# default
Now viewing "{{title}}" — present THIS slide's talking points now, do not navigate.{{close}}
